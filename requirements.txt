# Salary-Linked Expense Tracker and Budget Advisor
# Versions pinned for reproducibility across machines (section 5.11).
# Install with:  pip install -r requirements.txt
# Tested on Python 3.11.

# Web framework and templating (Presentation Layer)
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6

# Data processing
pandas==3.0.5
numpy==2.4.6

# Machine learning: categorisation (Naive Bayes / Random Forest, TF-IDF)
scikit-learn==1.9.0
joblib==1.5.3

# Time-series forecasting (ARIMA)
statsmodels==0.14.6

# The database is SQLite via the sqlite3 module in the Python standard library,
# so no database driver is required.
